import { gsap } from 'gsap';
import { ScrollTrigger, SplitText, ScrollSmoother } from 'gsap/all';

gsap.registerPlugin(ScrollTrigger, SplitText, ScrollSmoother);



export {ScrollTrigger,SplitText,ScrollSmoother}
